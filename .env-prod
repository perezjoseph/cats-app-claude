# Environment variables for production deployment
# These will be set in the Elastic Beanstalk console

# Optional: API key for TheCatAPI (https://thecatapi.com/)
CAT_API_KEY=

# AWS Configuration for Bedrock
# Note: In production, we'll use IAM roles instead of access keys
AWS_REGION=us-east-1
BEDROCK_MODEL_ID=anthropic.claude-v2
